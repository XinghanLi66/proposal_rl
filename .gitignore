# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Editor / OS ───────────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~
.cursor/

# ── Logs — all of them ────────────────────────────────────────────────────────
*.log
logs/

# ── Model weights & checkpoints ───────────────────────────────────────────────
*.safetensors
*.bin
*.pt
*.pth
*.ckpt
checkpoint-*/
final/
model_cache/

# ── Dataset & generated data files ───────────────────────────────────────────
*.jsonl
*.npz
*.npy
*.parquet
*.arrow
*.csv

# ── Experiment runs (checkpoints, evals, hparam results, config snapshots) ───
runs/dataset/
runs/model_cache/
runs/sft/
runs/grpo/
runs/rl/
runs/eval/
runs/exps/
runs/logs/
runs/benchmark/
runs/demo_saves/
runs/dlc_cmds/
runs/dlc_jobs.json
runs/ray_tmp/
runs/smoke_rl/
runs/smoke_sft/

# Keep the small pipeline orchestration scripts in runs/
!runs/_pipeline.sh
!runs/_resume.sh
!runs/model_path.txt

# ── Conda / pip envs ──────────────────────────────────────────────────────────
.env
.venv
env/
venv/
*.egg

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── Misc generated ────────────────────────────────────────────────────────────
.best_hparams
hparam_search.json

# ── Local/DLC operations helpers with workspace-specific details ─────────────
scripts/dlc_reruns/
scripts/coord_exp09_15_17.sh
scripts/find_best_quota.py
